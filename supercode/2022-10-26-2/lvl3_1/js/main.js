const wrapper = document.querySelector("body");

function getPink() {
    wrapper.classList = " ";
    wrapper.classList.add("pink");
}
function getTurk() {
    wrapper.classList = " ";
    wrapper.classList.add("turquise");
}
function getFire() {
    wrapper.classList = " ";
    wrapper.classList.add("fire");
}
